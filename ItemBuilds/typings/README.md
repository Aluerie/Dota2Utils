# ⚠️ Warning

These stubs are extremely lazily made - just enough to satisfy the type checker for this project.
Do not blindly copy-paste it anywhere else.

I didn't touch all methods/attributes and I don't know if the touched are correctly type-hinted because I'm not familiar enough with `vdf` format.
